packages:
  - apps/*
  - packages/*

allowBuilds:
  '@firebase/util': true
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
  protobufjs: true

# Required for `pnpm deploy` (Docker prod image) to bundle workspace deps on pnpm >= 10
injectWorkspacePackages: true
